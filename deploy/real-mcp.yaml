apiVersion: apps/v1
kind: Deployment
metadata:
  name: supportdesk-real-mcp
  labels:
    app.kubernetes.io/part-of: asago-supportdesk
    app.kubernetes.io/component: real-mcp
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: real-mcp
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: asago-supportdesk
        app.kubernetes.io/component: real-mcp
    spec:
      containers:
        - name: real-mcp
          image: supportdesk
          command: ["supportdesk-real-mcp", "--host", "0.0.0.0", "--port", "8081"]
          env:
            # Flip these to "1" to deploy the fixes, then delete and re-apply the Job.
            - name: SUPPORTDESK_TOOL_FIX
              value: "0"
          ports:
            - containerPort: 8081
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests: {cpu: 50m, memory: 128Mi}
            limits: {cpu: 500m, memory: 512Mi}
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
---
apiVersion: v1
kind: Service
metadata:
  name: supportdesk-real-mcp
  labels:
    app.kubernetes.io/part-of: asago-supportdesk
spec:
  selector:
    app.kubernetes.io/component: real-mcp
  ports:
    - port: 8081
      targetPort: 8081
